{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un utilisateur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('app_utilisateue_list') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}
